

body {
    background: white;
    padding-top:65px;
}

.defblock {
    display: flex;
    flex-direction: row;
}

.ilinetoken {
    float: left;
}

.ilinewrapper {
}

input[type="text"] {
    flex: 1;
    box-shadow: none;
    border-radius: none;
    border: none;
    outline: black;
    -webkit-appearance:none;
    -moz-appearance:none;
    appearance:none;
}

.iline {
    flex: 1;
    width: 100%;
    height: 1.5em;
    box-shadow: none;
    border-radius: none;
    border: none;
    outline: black;
    -webkit-appearance:none;
    -moz-appearance:none;
    appearance:none;
}

.didja input[type="text"] {
}

.ilinebutton{
  display: block;
  margin-top: -4px; /* Recover part of margin set in child Header nodes */
  margin-left: 10px;
  font-size: 20px;
  font-weight: 200;
  color: #777777;
  text-shadow: 0 1px 0 #ffffff;
}

.ilinebutton button{
    padding: 0 0 0 0;
}

/*
.depth0 input[type="text"] { color: blue; }
.depth1 input[type="text"] { color: green; }
.depth2 input[type="text"] { color: purple; }
.depth3 input[type="text"] { color: brown; }
.depth4 input[type="text"] { color: orange; }
.depth5 input[type="text"] { color: aqua; }
.depth6 input[type="text"] { color: grey; }
.depth7 input[type="text"] { color: pink; }
*/

.depth0 { color: blue; }
.depth1 { color: green; }
.depth2 { color: purple; }
.depth3 { color: brown; }
.depth4 { color: orange; }
.depth5 { color: aqua; }
.depth6 { color: grey; }
.depth7 { color: pink; }
